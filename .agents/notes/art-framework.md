# 美术框架：索引与不变式

> 这份文件只干两件事：**列出不变式**，和**告诉你去哪儿找细节**。
> 细节按主题分九篇放在 `art/` 下；**现状与下一步只看 `art/09-handoff.md`**。

## 不变式（每条都付过代价）

**判据与仪器**

- **判据要在正确的仪器上取。** `cargo check` 过了 ≠ 探针跑过了；低分辨率量到的是「地板」不是 shader；
  算子计数只能用来找嫌疑人，不能当成绩；看日志要看全，`-Last 12` 会把关键错误挤出视野。
- **任何「跳过」都是判据的敌人**，尤其是当它守着**唯一**判据的时候。探针拿不到设备就 `exit(2)`，
  不许静默变绿。
- **审计不能因为走缓存而哑掉**：缓存可以省重算，不能省仪器。
- **梯度对错的唯一判据是 arbiter**（`px_probe --bin field_dual`）。差商这条腿已退场。
- **证据分级**：A 级（实测 / 官方来源）可直接引用；B 级（未核实）**用前必须复核**。
- **「改尺寸再改回来必须逐字节回到原样」**是一条免费的强断言。

**身份与键**

- **键 = 内容。** 缓存键少一个维度就是「同一个键、不同内容」。
- **指纹为 0 的旧产物不缓存**：键里少了「内容」这一维时，命中就等于认错了东西。
- **图里的节点名不许随手改**：`diff` 按节点名配对，改了只会看到「一个新增一个移除」。
- **协议形状一变 ⇒ `protocol_hash` 变 ⇒ 在跑的旧服务立刻被握手拒掉** —— 这是设计，不是坏了。
- **快照 JSON 的键是排序的**，手改必错：一律 `PX_UPDATE_SNAPSHOT=1 cargo test -p px_protocol --test snapshot`。

**进程与流程**

- **改代码前先停服务**（exe 被占用 ⇒ `cargo build` 报「拒绝访问」）：删 `target/render-server.json` 即可。
- **不要自动拉起服务**：服务成了客户端的子进程 ⇒ 调用方会一直挂下去，而服务端日志显示一切正常。
- **做完一个功能必须把窗口调出来给用户 review**，且**起窗口必须脱离**（`Start-Process` 不带 `-Wait`）。
- **只有 `.wgsl` 改动不要重启**（约 1 秒热重载）；改 uniform 结构才必须。
- **交接清单里的每条命令，都要回到它自己那一节读用法。**

**渲染器与环境**

- **不要用固定帧数预热**；就绪门必须 `total > 0 && pending == 0`。
- **`AmbientLight` 在 Bevy 0.19 是相机组件**（挂在实体上等于挂在相机上）。
- **`#import` 只内联点名的符号** ⇒ 测试侧严格宽松于运行时；shader 运行时正确性唯一的仪器是 viewer 的 stderr。
- **`looking_at` 的 up 与视线共线会退化** ⇒ 两极视角停在 82°，别顺手改成 90°。
- **平台已经定义了标准格式时，不要手搓等价物。**

**仓库习惯**

- **CRLF/LF 在本仓是混的**：批量改文件前先 `grep -qU $'\r'`；perl 多行正则写 `\r?\n`。
- **长命令会被截断**：大文件分块写，写完立刻 `wc -l` + `grep 关键符号` 校验。

## 九篇

| 文件 | 讲什么 |
|---|---|
| `art/01-decisions.md` | 生效的路线裁决（Rust 栈 + 协议 crate、静态链接、不烘体），以及被否掉的路线各因为什么 |
| `art/02-pcg.md` | PCG 层：算子表、图程序、参数、CAS、缓存键、版本号失效 |
| `art/03-assets.md` | 产物与协议：`.pxart` 里有什么、四种域、载荷指纹、相机表、`diff` |
| `art/04-geometry.md` | 球面网格、位移与法线、mip 链、接缝与极点 |
| `art/05-sky.md` | 天空与大气（含 Bevy 散射大气的挂起状态与下次怎么验） |
| `art/06-clouds.md` | 体积云：覆盖度立方图、云壳、消光、已知边界 |
| `art/07-gradient.md` | 云密度场的梯度：正确组装形式与它的判据 |
| `art/08-instruments.md` | 渲染器怎么跑（离屏 / 服务 / viewer）＋ 改完怎么验 ＋ 资源缓存 |
| `art/09-handoff.md` | **现状、已验证 / 未验证、下一步**（每轮开工前先读它） |

## § 号 → 文件

代码注释按 § 号引用本文档，所以**原节号一律保留**（允许断续）。下表只列还活着的号：

| § | 在哪儿 |
|---|---|
| §2 §3 §4 §5 §7 §9 §10 §18 §24 | `art/01-decisions.md` |
| §14–§17（含 §17.1）§19 §20 §21 §22 | `art/02-pcg.md` |
| §23 §25 §28 §31 §48 | `art/03-assets.md` |
| §26 §27 §29 §30 §32 §33 | `art/04-geometry.md` |
| §34 §35 §36 | `art/05-sky.md` |
| §39 §43 | `art/06-clouds.md` |
| §44 §45 §46 | `art/07-gradient.md` |
| §12 §13 §37 §40 §41 §42 §47 §50 | `art/08-instruments.md` |
| §49 | `art/09-handoff.md` |

## 怎么用（三条命令）

```powershell
.\tools\px.ps1 -Target test                    # 快速测试链（默认 members，不碰 bevy）
.\tools\px.ps1 -Target device                  # 最便宜的 GPU 门（约 10 秒）
.\tools\px.ps1 -Target field_dual              # 梯度对错的唯一判据
```

## 关于被删掉的节

这里原来是一份 3300+ 行的实验笔记（§0–§50），**选型过程、里程碑流水账、被推翻的中间方案已经删掉**，
只留最终状态。被删或已被吸收的：§0 §1（TS 时代的结论与盘点）、§6 §8（web 时代的门禁与调研清单）、
§11（P0 流水账）、§13.1/§13.3、§20–§22 的过程、§23 的调研表与 A–E 路线图、
§25.4、§28.1 的排查链、§30.1 的 `probe.ps1` 用法（已被 `--sheet` 取代）、§38（旧交接）、
§39/§45/§47/§48/§50 的「完成记录」外壳。**内容都在 git 历史里**：

```
git show 0071f5d:.agents/notes/art-framework.md
```
