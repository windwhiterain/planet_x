[package]
name = "px_graphs"
version = "0.1.0"
edition = "2024"

[dependencies]
px_ops = { path = "../px_ops" }
px_protocol = { path = "../px_protocol" }
px_verify = { path = "../px_verify" }
px_mc = { path = "../px_mc" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
# genprobe（临时仪器）要跟 target/legacy-gen 的 SHA256 基线对账 —— 临时依赖，随仪器一起删。
sha2 = "0.10"
