//! **场域**的算子库：七个算子 + 一个 dylib 入口。
//!
//! ⚠ 它不认识驱动、不认识 CAS、不认识 `art/`：进来的是**序列化的载荷**，
//! 出去的是同一个形状。这就是「算子之间只通过 schema 说话」。
//!
//! ⚠ 底下这三行宏**就是** dylib 那一侧的全部管道（描述符 / 规范化 / 分派 / 入口符号）：
//! 它们只吃算子类型，参数类型从 `PxOp::Params` 抽、输入从 `PxOp::Inputs` 推。
//! 于是"加一个算子"在这份文件里一个字都不用写。

pub mod noise;
pub mod ops;
pub mod typed;

