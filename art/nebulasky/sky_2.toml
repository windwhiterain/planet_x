# `sky.nebula`：沿视线积分，出一条通道。
#
# `face` 必须与图脚本的 --face 一致：它是**输出**立方贴图的分辨率
# （画布也是这个数 ⇒ 对不上会在形状检查那里当场报）。
face = 64
channel = 2
steps = 96
jitter = 1.0
star_gain = 1.0
background = [0.0, 0.0, 0.0]
# 星图的哪一档算数（星图里亮纹素的比例很小，门限决定"有几颗星"）。
star_floor = 0.4
