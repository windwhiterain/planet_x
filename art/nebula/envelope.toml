# 大尺度包络：**低频**（0.35，而 blobs 是 1.4）⇒ 决定"这一大片天区有没有气"。
#
# ⚠ 已有的 `extent` 与密度**同频**（同一个 warped 场取阈值）⇒ 掩码跟着同一批斑块走
#   ⇒ 换哪个方向看都是"同一类云换个亮度"（统计均匀）。用户那句"六面 pattern 一样、
#   只是亮度不同"点破的就是它；量下来六面相关只有 0.23~0.56，所以不是同一张图，
#   是**没有构图**。低频这一层才是构图：核心、边缘、大片空掉的方向。
frequency = 0.35
octaves = 3
lacunarity = 2.0
gain = 0.5
seed = 9173
zonal = 0.9
