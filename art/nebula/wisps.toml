# `field.ridged3`：星云的**丝**（脊状噪声的等值线就是纤维）。
#
# ⚠ 频率比 `blobs` 高一个档（4.5 对 1.6）：脊是**长在**团块上的结构，要在团块的尺度之下。
#
# ⚠ `sharpness` 越大脊越细：`1.0` 是三角波（宽脊），`2` 以上是一根根细丝。
#   参考图里的纤维是细的 ⇒ 先给 2.6。
frequency = 11.0
octaves = 6
lacunarity = 2.1
gain = 0.5
seed = 20461
sharpness = 2.6
zonal = 0.8
