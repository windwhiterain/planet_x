# `field.warp3`：**三轴域扭曲** —— 把"团块"搅成"丝絮"的那一步。
#
# ⚠ 这是把圆滚滚的疙瘩变成被拉长、打卷、撕裂的结构的关键：单靠 fbm 得到的是各向同性的球。
#
# ⚠ `strength` 的单位是**体素坐标**（`1.0` = 挪一整面）：0.35 就是"最多挪三分之一面"，
#   足够把团块撕开，又不会把结构整体移位。给到 1 以上会变成一团糊的空间错切。
strength = 0.35
# 轴向权重：`0` = 只沿径向挪、`1` = 三个轴等权。星云的结构在三个方向上都要扭曲。
axial = 1.0
