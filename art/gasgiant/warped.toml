# 域扭曲：把 `bands` 按 `swirl` 推歪 —— 这一条就是"别像个西瓜"的机制。
# ⚠ `strength` 是位移的**弧长**（占半径的比例，球面档极区自动按 1/sinθ 换算）：
#   它管**东西向**的推移（把条带拉成流线、推出分叉）；`lateral` 管**南北向**（让带边界上下起伏）。
#   ⇒ 要"不规则"，两个都要给：只给东西向会变成整齐的斜纹，只给南北向就是正弦波。
strength = 0.16
lateral = 0.85
probe = 0.06
