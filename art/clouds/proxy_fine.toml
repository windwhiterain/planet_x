# 真场代理的 mesh：拿 `coarse_fine` 那张体积网格出等值面。
#
# 这个文件是「只改等值面参数」的那一侧：改 `depth` 只该让 mesh 重算，
# 上游的体积网格（129² × 65 层，最贵的那一步）必须照命中。
#
# depth 决定 mesh 解得出多细的形状：depth 5 只有 33 个采样点/轴，在 129² 的网格上
# 会欠采样（一个波长只落 1.3 个点）⇒ 细节被折回成假几何。depth 6 = 65 点/轴。

level = 0.0
depth = 6
weld = 1e-4
