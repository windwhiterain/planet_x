# 判据的靶子（anchor）

> ## ⚠⚠ 一个名字、两个所指，用日期切开（§157，2026-09-19）
>
> **从 `.agents/notes/art/15-render-wgpu.md` §157 那一笔起**（改名由那一笔落，提交 sha 由评审回填）：
>
> - **`px_render` = 现在的唯一宿主**（裸 wgpu）。它在 §157 之前叫 **`px_render_wgpu`**
>   —— 那是一次**纯改名**：`art/` 下一个字节都没动，六张图 + 六份冻产物**逐字节不变**（§157 有读数）。
> - 而**笔记 §100–§156、`git` 历史、以及源码里 `px_render::…` 形态的出处指针**，那里的
>   `px_render` 一律指**已删的 Bevy 宿主**（`f121ee3` 删除；源码取法 `git show f121ee3^:px_render/…`）。
>
> ⇒ **读旧记录时按旧义读，读代码与 §157 之后的记录时按新义读。** 这一条必须写在最前面：
> "一个名字两个所指"是那种**会悄悄改掉一份决策记录**的东西。
> ⚠ 而 `art/shaders/ring.wgsl` 与 `art/shaders/clouds.wgsl` 里那两处 `px_render::reflect`
> **在产物里**（进闭包指纹）⇒ **不许改**（§155.4）；按旧义读（= 已删的 Bevy 宿主，
> 今天的反射住在 `px_shader::reflect`）。

这个目录里的东西**不是**资产、**不是**产物、**不是**仪器 —— 它们是**判据本身**。

| 文件 | 是什么 |
|---|---|
| `orbit-bare.png` 等六张 | **J1 的判据图**：`orbit-bare` / `orbit-bare-nolight` / `orbit-bare-shadow` / `orbit-rings` / `orbit-soft` / `orbit-proxy-fine-bound`，960×640 |
| `frozen/*.pxart` 六份 | **老形状**（`--no-frame-graph`）的冻产物，锚宿主读得懂的那种；「逃生门」那一行判的就是它们的**文件字节** |
| `hashes.txt` | 登记在案的读数（含出处与量法） |

## ⚠ 为什么它们在 git 里，而别的仪器在 `target/`

**因为 2026-09-18 那一次，`target/oracle/` 整个不见了。**

丢的：锚 exe（`D7ED54FDB8323EDD…`）、`hashes.txt`、`target/oracle/*.ps1` 那一套仪器、
`pxart-frozen/` 那六份副本。
**活下来的**：六张判据图（当时恰好也在 `target/j1/` 下留了一份）、以及六份冻产物
（`target/pcg` 是内容寻址的，用 `--bin scene <名> --no-frame-graph` 重烘，
**文件字节的 sha256 与登记值逐字相同**）。

⇒ 由此立的规矩：

> **一份能被删掉的判据，不是判据。**

`target/` 是 git 忽略的、随时可能被清掉的目录 —— 把**靶子**放在那里，
等于让"这个工程能不能被验证"取决于一次 `rm -rf`。
所以靶子进仓库；**仪器**（脚本、探针、读数）仍然留在 `target/`，因为它们可以重写，靶子不能。

⚠ 而**锚 exe 找不回来了**：它是**冻结的构建产物**，重建出来的不是同一个字节序列，
而"冻结"正是它作为 oracle 的全部意义。它的**读数**都记在 `hashes.txt` 与
`.agents/notes/art/*.md` 里，所以已定的判据不受影响；**但"向 oracle 提问"那个能力没了**
（§143 靠它测出了透明相位的次序规则）。要用就得重建一支，而重建的那支**必须先把这六张图重出一遍**、
逐字节对上，才够格当 oracle。

> ### ⚠ 上面这一段里有一句**是错的**，而它被 §157 的实测推翻了（三条并存的事实）
>
> 1. **"锚 exe 找不回来了"—— 成立，不改。** 被钉住的那一支 `target/oracle/px_render-bevy.exe`
>    （`D7ED54FDB8323EDD…`）确实**已丢失且不可重建**（重建不是同一个字节序列）。
> 2. **⚠ 但"向 oracle 提问那个能力没了" —— 在写下时就不成立。** §157（2026-09-19）实测：
>    `target/debug/px_render.exe`（sha256 `EB8F85452C8B2E291B6714BA04325AF784565F7F335E4BC3265E472F68CE38C7`，
>    164,638,720 B，mtime 10:51）是**一支还能跑的 Bevy 宿主** —— 起来 `--serve`、拿
>    `art/anchor/frozen/*.pxart` 出图，**六张全部与这里的六张判据图逐字节相同**
>    （`63184151909371A5` / `7BBB18CE3612D4F7` / `C03FFF3235264DD5` / `B5799E4F1649535C` /
>    `FA20FAD37BC61EA2` / `32872F80AC867BE3`，字节数也逐格相同，失败 0/6）。
>    ⇒ 按**本节自己立的门槛**（"重建的那支必须先出这六张图、逐字节对上，才够格当 oracle"），
>    **它够格** ⇒ "能力没了"是假话，能力当时活在一支**未登记**的 exe 里。
> 3. **而决定是：不留它**（用户裁决：「该做的功能都做完了，留着 oracle 干嘛」）。理由：
>    oracle 的唯一用途是"移植期间问 Bevy 它怎么做"，那件事做完了；靶子在 git、源码在
>    `f121ee3^` ⇒ 留 164 MB 是给一个**不存在的消费者**留（同族：§142 不落没有消费者的
>    `MaterialInstance{params?}`）。⇒ **§157 起，上面那句"能力没了"成立** —— 但不是因为它丢了，
>    而是因为**我们选择不留**：改名 `px_render_wgpu → px_render` 覆盖了那个路径
>    （覆盖是对的：`harness.ps1` 的 `$HarnessExe` 就指这个名字，留着会把门驱动到一支 bevy exe）。
>    ⚠ 那份副本（`target/pre-rename/px_render-bevy-EB8F8545.exe`，sha256 与原件逐字节相同）
>    **是未登记的临时副本，不是资产** —— 随时可以被清掉，**谁都不要依赖它**。
> 4. **要拿回来的路**（§157 只读核过）：源码完整在 `f121ee3^`（`px_render/` 21 个文件，
>    含 `Cargo.toml` 与 `tests/`），bevy 一族的依赖锁也在那一笔的 `Cargo.lock` 里。
>    命令见 `15-render-wgpu.md` §157「拿回 oracle 的路」。⚠ 一条也不是"重建即可"：
>    重出的那支必须重跑上面第 2 条那六格、**逐字节对上**才算够格。

## 怎么用

```powershell
# J1：我们的宿主出图 → 与这里的六张逐字节比
cargo run -q -p px_graphs --bin scene orbit-bare
cargo run -q -p px_render -- --offline --scene <产物> --out x.png --width 960 --height 640
# x.png 的 sha256 前 16 应当等于 hashes.txt 里那一格

# 逃生门：老形状产物的**文件字节**哈希
cargo run -q -p px_graphs --bin scene orbit-bare --no-frame-graph
# 拿到的 .pxart 的 sha256 前 16 应当 == 1845BAEE444D1DB3（orbit-bare；六格全部见 hashes.txt §三）
```

⚠ **2026-09-20：§三 那六格重登记过**九次**。第九次（第 8 轮）的根因是**环材质改成了受光/采影**
（`ring.wgsl` 的键变了）⇒ 只有 `orbit-rings` 那一格变；**J1 六张 6/6 逐字节不变**
（关影档退化成原行为）。

⚠ **2026-09-20：§三 那六格重登记过**八次**。第八次（第 4 轮）的根因是**场域多了一个算子**
（`field.stamps`：`px_field_schema` 多一条声明、`px_field_op` 多一个实现）⇒ 吃场算子的
节点键全换；同轮判据：**旧四张图 30/30 内容逐字节不变**、**J1 六张 6/6**。

⚠ **2026-09-20：§三 那六格重登记过**七次**。第七次（第 2 轮）的根因是 **`px_mesh_op` 的源码变了**
（把「法线朝内」从打印改成硬失败）⇒ 网格节点键变 ⇒ 文档字节变；但**网格内容逐字节没变**、
**J1 六张逐字节 6/6**。详情见 `hashes.txt` §三。

⚠ **2026-09-20：§三 那六格重登记过**六次**。第六次（本 worktree `planet-art`）的根因是
**新增场地算子 `field.craters` + `FieldFn::value` 多了两栏**（节点参数、球面方向）⇒ 吃场算子的
节点键全换；同轮判据：**J1 六张一位没动**、**旧四张图 30/30 节点内容逐字节不变**。
⚠ 顺带实测到的一件事：**第五次那六个登记值本来就与这一份 worktree 对不上**
（把改动前的旧键写回图清单、再用今天的代码重烘 ⇒ `DF84B7EF…`，不是 `EBCD0389…`；
主工作副本的暖清单里是更早一代 `2795F948…`）⇒ 那六格是在**第三份状态**量的，
即下面"行尾约定"那一格说的机制。全文见 `hashes.txt` §三。

⚠ **2026-09-19：§三 那六格重登记过**五次****（旧值 `2795F948…` / `2A7B42A7…` / `34B17990…` /
`5D120324…` / `B49C4262…` 那几批作废）。理由都在"身份的覆盖面"上：指纹跳过 path 依赖、
指纹漏 proc-macro、以及最近这一次（契约加装载入口与工具链握手 + 体积域算法搬进 `px_volume_alg`
+ 指纹算法搬进 `px_fingerprint`，最后又修了 `ops::open` 的一条路径口径缺陷）。
量法与"为什么 J1 六张一位没动"写在 `hashes.txt` §三 与
`.agents/notes/art/18-operator-libraries.md` §168/§171、`19-generic-inst.md` §180。

⚠ **还有一格前提：盘上那些字节的行尾约定。** 产品键哈希的是**盘上的源码文本**（闭包指纹，
`hashes.txt` §六），所以"换一份 checkout 约定不同的副本"能**一个像素都不动而换掉所有键**
（`hashes.txt` §三 那六格当场破）。⚠ 本副本里两种行尾**并存**（LF 在 index、盘上有的 CRLF
有的 LF）⇒ 别一句"这份是 CRLF"带过，量法与逐文件读数在 `hashes.txt` §六。

⚠ **老形状是 oracle 的母语，不是交付的形状。**
它今天只有"取 oracle 读数"和"逃生门判据"两个正当用途；
产品形状是**帧图**（`passes` 由帧图配方生成）。
本宿主**读不了**老形状（结构性地缺 `scene_depth`），这是**已知且正确**的
——能读老形状的只有锚，而锚已经不在了。

## 判据的两半（§146.3 立）

1. **判据的输入必须是刚烘出来的产物**（`manifest.json` 可能是陈旧的，实测过一次五档全报"没有资源"）；
2. **而夹具/靶子必须住在仓库里** —— 两半合起来才回答得了"我跑的是哪份产物"**和**"下一个人怎么跑出同样的东西"。
