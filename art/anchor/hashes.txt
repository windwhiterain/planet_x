# 判据靶子的登记表（art/anchor/）
#
# ⚠ 这份表是"记录"，不是"证据"。每一条都要能**重跑出来**才算数（§131.2）。
# ⚠ 量法必须连"拿哪份产物量的"一起写 —— 这一条在本工程被咬过四次。
# ⚠ 还有一格量法：**盘上那些字节的行尾约定**（§六）。产品键哈希的是盘上的源码文本，
#   所以"换一份 checkout 会不会变"是这份表的前提，不是细节。
# ⚠ §157（2026-09-19）：下面各条量法里的包名从 `px_render_wgpu` 改成 **`px_render`**
#   （宿主 crate 改名，`15-render-wgpu.md` §157）。⚠ 那是一次纯改名：本表**一个读数都没动** ——
#   六份冻产物的文件字节与六张判据图的 sha256 改名前后逐格相同（§157 有前后两遍的读数）。

## 一、J1 判据图（整份 PNG 的 sha256 前 16）

# 量法：
#   cargo run -q -p px_graphs --bin scene <档>
#   cargo run -q -p px_render -- --offline --scene <产物> --out x.png --width 960 --height 640
#   (Get-FileHash x.png -Algorithm SHA256).Hash.Substring(0,16)
#
# ⚠ 两半都不能省：产物必须是**刚烘的**，图的字节数要一起核（字节数是"图没被换过"的第二道锁）。

orbit-bare             = 63184151909371A5   300012 B   960x640   不给 --cam
orbit-bare-nolight     = 7BBB18CE3612D4F7   215193 B   960x640   不给 --cam
orbit-bare-shadow      = C03FFF3235264DD5   298289 B   960x640   不给 --cam
orbit-rings            = B5799E4F1649535C   508562 B   960x640   不给 --cam
orbit-soft             = FA20FAD37BC61EA2   358066 B   960x640   不给 --cam
orbit-proxy-fine-bound = 32872F80AC867BE3   405380 B   960x640   不给 --cam

# 出处：§105（S-1 用**锚宿主**在六份**冻产物**上取的；锚 exe sha256 D7ED54FDB8323EDD…，
#       Vulkan 后端，960×640，不给 --cam）。⚠ 那一支锚 exe 已于 2026-09-18 丢失（见 README）。
# 复现人：本表每个数都由 reviewer 在 2026-09-18 用**当时的源码**重出并逐字节比过。

## 二、J2 对照图

# 量法：--sheet（4 列 × 3 行），12 台相机来自产物自带的相机表。
sheet                  = A94F9F2D1437C06C   3159948 B   3840x1920
# ⚠ 老形状与新形状**都出这个哈希**（2026-09-18 两侧各出一次，逐像素 0 / 7372800）。
# ⚠ 12 格**共用同一批全尺寸中间纹理**、逐格 set_viewport —— "12 次独立出图再拼"实测差 49–289 像素（§149）。

## 三、逃生门：老形状冻产物的**文件字节**哈希

# 量法：
#   cargo run -q -p px_graphs --bin scene <档> --no-frame-graph
#   (Get-FileHash <产物> -Algorithm SHA256).Hash.Substring(0,16)      ← **文件字节**，不是文件名的 CAS 键
#
# ⚠ 这一格我自己量错过一次：拿 `Split-Path -Leaf` 的 CAS 键（28a9b516c132…）去比，
#   于是六格全报 ✗ —— 而真值是文件字节的 sha256。**"量的是哪个量"与"量了什么"一样重要。**
#
# ⚠ **2026-09-19 第三次重登记**（同一 worktree，`.agents/notes/art/19-generic-inst.md`）：
#   六格又全换 —— 这一轮是**泛型实例**（`px_inst!`）带进来的结构改动，三处都动了"身份的覆盖面"：
#     ① 契约层加装载入口 `ops::load_at` + **工具链握手**（`__toolchain_hash`）；
#     ② 体积域的泛型算法从 `px_volume_op` 搬进 `px_volume_alg`（rlib），实现库变薄壳；
#     ③ 指纹算法从"一段被 include! 的源码"变成真 crate `px_fingerprint`（rlib）。
#   ⇒ 与 §168 那两次同性质：**键变了不是坏事，但每次都得说清是哪条覆盖面变了**。
#   同一轮实测：**产物逐字节仍 28/28**（planet 6/6、desert 8/8、clouds 14/14）。
#   下面是重烘后逐格实测的值（同一档连跑两次同值）：
#
# ⚠ **2026-09-19 第四次重登记**（同一 worktree）：根因是**共享的指纹助手 `px_fingerprint` 被改了**
#   （给它补了 `cargo:rerun-if-env-changed=RUSTFLAGS/PROFILE/TARGET/RUSTC` —— 没有它，工具链握手
#   对"改 flag"是瞎的）。而 `px_fingerprint` 是三个实现库的 `[build-dependencies]` path 依赖
#   ⇒ 它的源码在**它们的名册**里 ⇒ 它改一个字符，**三个库的身份全换、所有节点键全换**。
#   **实测**（探针，逐字节可往返）：只给它追一行注释 ⇒ `field.fbm` 的节点键
#   `6ea07924bf66` → `fb3de5e8b797`、planet 6/6 重算；撤回 ⇒ 键回到 `fb3de5e8b797` 且**命中**
#   （同一份产物，CAS 按内容寻址）。
#   ⇒ 这条性质要记住：**"算 key 的代码本身就是身份的一部分"**（`19` §180 已记过；谁下次去改
#   `px_fingerprint` 里一个错别字，就会再全换一次键）。
#   同一轮产物仍**逐字节 28/28**（planet 6/6、desert 8/8、clouds 14/14）、**J1 orbit-bare 一位没动**。
#
# 六格（冻结态连跑两次同值）：
# orbit-bare             = 9EE4F970C5BAE421   （4125 B）
# orbit-bare-nolight     = 1EE3F4473D4A4107   （4137 B）
# orbit-bare-shadow      = 7BAB2A5CB65090B5   （4139 B）
# orbit-proxy-fine-bound = CE478245265FE24B   （5750 B）
# orbit-rings            = 80F873EA35AFAD29   （4905 B）
# orbit-soft             = B066188B40EAE5B6   （5721 B）
#
# ⚠ **第五次重登记**（field 域泛型实例那一轮）。根因与第四次不同：这一轮把 field 的算法
#   从 `px_field_op`（dylib）拆进新的 `px_field_alg`（rlib，泛型实例库要链它）⇒
#   **`px_field_op` 的名册变了 ⇒ 它的身份换了 ⇒ 吃 field 算子的节点键全换**。
#   代价换来的东西：field 域也有真单态化实例了（`field.remap/waves`，`art/inst/waves.rs`）。
#   判据：**J1 六张逐字节一位没动**（像素不受键影响）、产物 **28/28** 逐字节（用"改动前"
#   那一版构建先把三张图的 28 份 `.pxart` 快照下来做对照 —— `target/baseline/*.json` 里
#   clouds 那 14 个旧键的产物今天已经不在 `target/pcg/ab/` 里了，只能到 14/14）。
#
#
# ⚠ **2026-09-20 第六次重登记**（本 worktree `planet-art`，`feature/planet-art-graph-pipelines`）。
#   根因 A（这一轮自己的）：**新增场地算子 `field.craters`** ＋ **`FieldFn::value` 多了两栏**
#   （节点参数、球面方向）⇒ `px_field_schema` / `px_field_op` / `px_field_alg` 的名册全变
#   ⇒ 吃场算子的节点键全换 ⇒ 冻产物字节变（它们内嵌成员键与指纹）。
#   判据（同轮实测）：**J1 六张逐字节一位没动**（§一 六格，含字节数 6/6）；
#   **旧四张图的 30 个节点内容逐字节不变**（planet 6 / desert 8 / clouds 14 / field_remap 2，
#   与 `target/baseline/pre.json` 那 37 份内容哈希对账：0 个不符）。
#   ⚠ 根因 B（**与这一轮无关，但必须记下来**）：**登记的那六格本来就与这一份 worktree 对不上**。
#   实测法：把改动前的旧键（`target/baseline/planet.json` / `clouds.json` 的快照）**写回**两份
#   图清单、再用今天的代码重烘 ⇒ `orbit-bare = DF84B7EF92F7A84E（4126 B）`，
#   **不是**登记的 `EBCD0389…`；而主工作副本自带的暖清单里躺着的是更早一代 `2795F948…`。
#   ⇒ 登记值是在**第三份状态**（写它的那份副本、第五次重登记时）量的：§六 那条
#   "行尾约定 ⇒ 键变 ⇒ §三 全红"正是这一格的机制。**按本副本重登记**是唯一可复现的做法。
#   ⚠ 纪律照旧：先冻源码，再量 anchor（这一轮的六个值在同一冻结态下连跑两遍同值）。
#
# ⚠ **2026-09-20 第七次重登记**（本 worktree `planet-art`，持续优化第 2 轮）。
#   根因：**`px_mesh_op` 的源码变了** —— `cubesphere.rs` 把"顶点法线朝内"从一条**打印的审计**
#   改成**硬失败**（实测触发：`moon` 图 `displace` 0.045 → 0.075，审计从"朝内 0、最小点积 0.783"
#   变成"朝内 51858、最小点积 -1.000"，渲染出来是一颗黑球，而那一趟**什么错都没报**）。
#   `px_mesh_op` 是 `mesh.cubesphere` / `mesh.proxy` 身份的一部分 ⇒ 网格节点键全变 ⇒ 冻产物字节变。
#   ⚠ 同轮实测：**网格内容逐字节没变**（`planet/surface` `f7b2d58f979e → bb21a15b5616`、
#   `desert` `2bca0ecfaba9 → ebce36aa67b2`、`moon` `5dcfdd877d67 → 10e8bb5e6484`；
#   三份 `.pxart` 的 sha256 前后相同）⇒ 这正是"**键 = 身份 + 内容**"：身份换了要重算，
#   内容没换就不会变。⇒ 这一条也说明"改实现库 ⇒ §三 全变"**不等于**"画面会变"。
#   判据：**J1 六张逐字节 6/6**（这一轮重新出过图 —— §三 已经不能当像素判据了，见根因）。
#
# ⚠ **2026-09-20 第八次重登记**（本 worktree `planet-art`，持续优化第 4 轮：**盖章式打坑**）。
#   根因：**`px_field_schema` 多了一个声明**（`field.stamps`）＋ **`px_field_op` 多了一个实现**
#   （`ops/stamps.rs`，另给 `noise` 加了一扇 `cell_hash`）⇒ 场域那几个库的名册全变 ⇒
#   **吃场算子的节点键全换**（连**实例 key** 也换：`px build` 那三条重编了一遍）。
#   判据（同轮实测）：**旧四张图 30/30 节点内容逐字节不变**（与 `target/baseline/pre.json` 对账）、
#   **J1 六张逐字节 6/6**（重出图）。⇒ 又一次"键 = 身份 + 内容"：身份换了要重算，内容没换就不会变。
#   这一轮换来的东西：`field.stamps`（印章式打坑：幂律尺寸 + 年龄序挖掘式压盖 + 上游场当密度遮罩），
#   月球的坑从"均匀的小凹点"变成"大小不一、互相压盖、月海处稀疏"。
#
# ⚠ **2026-09-20 第九次重登记**（本 worktree `planet-art`，第 8 轮：**环接受阴影**）。
#   根因：**`art/shaders/ring.wgsl` 的源码变了** —— 环从"不受光、不采影"改成"采一次点光
#   shadow map + 影里留一档 `ambient`"（用户："星环没接受影子吗"）。这份 shader 的键变
#   （`fcf9f5f88282` → `2d0e50144f2a`）⇒ **`orbit-rings` 那一格**引用的成员换了 ⇒ 文档字节变
#   （+13 B：材质多了一栏 `ambient`）。**其余五格一字未动**（它们不吃这份 shader）。
#   判据（同轮实测）：**J1 六张逐字节 6/6**——包括 `orbit-rings`：那一档 `shadows = 0`，
#   关影时新代码正好退化成原行为（`mix(ambient, 1.0, 1.0) = 1`）⇒ 像素一位没动。
#   ⇒ 又一次"**键 = 身份 + 内容**"，而且这一回连"内容"都只是**加了一栏**，画面在关影档不变。
#
# ⚠ **2026-09-20 第十次重登记**（本 worktree `planet-art`，第 9 轮：**`field.fbm` 的各向异性**）。
#   根因：**`px_field_schema` 多了一栏**（`fbm.zonal`）＋ **`px_field_op` 的实现变了**（用它）⇒
#   场域那两个库的名册变 ⇒ 吃场算子的节点键全换（连实例 key 也换，`px build` 重编）。
#   ⚠ `zonal` **默认 1.0**，而默认值下这一栏**逐点等价于旧行为**（`px_field_op` 里那两条测试之一
#   就是逐点比对的，最大偏差 0）⇒ 同轮实测：**旧四张图 30/30 节点内容逐字节不变**、
#   **J1 六张逐字节 6/6**。⇒ 第十次"键 = 身份 + 内容"：身份换了要重算，内容没换就不会变。
#   这一轮换来的东西：噪声可以**沿经度拉长**（气态巨行星的湍流是流线/长条，不是各向同性的疙瘩）。
# ⚠ 更早七批（`2795F948…` … `D579241A…`）是**历史值**，留着是因为 §六 那一格要拿它们讲
#   "换一份 checkout 的字节约定会当场全红"。**今天生效的是下面这六行。**
#   ⚠ 行尾那一格也咬到本表：本 worktree 是**新 checkout**，`art/shaders/blit.wgsl` 这类
#   在本机是 CRLF（1581 B）而原工作副本盘上是 LF（1554 B）⇒ "同一个 recipe 的文档字节"
#   在两个副本之间本来就会不同。J1 不受影响（行尾不改像素），§三 的值属于**本副本**。
# ⚠ **纪律**：**先把源码冻住，再量 anchor**（这一轮我破了四次；`px_graph_schema` 与
#   `px_fingerprint` 这种被所有实现库共享的源码，量完再动一行就当场全作废）。

orbit-bare             = 9EE4F970C5BAE421
orbit-bare-nolight     = 1EE3F4473D4A4107
orbit-bare-shadow      = 7BAB2A5CB65090B5
orbit-proxy-fine-bound = CE478245265FE24B
orbit-rings            = 80F873EA35AFAD29
orbit-soft             = B066188B40EAE5B6

## 四、J3 pass 表（判据图在 `art/passes/` 那七份配方上跑）

# 量法：cargo run -q -p px_graphs --bin passes -- <基准产物> <配方> <出>  然后出图
none             = 63184151909371A5   300012 B
invert           = 733408200119C203   176865 B
invert_vignette  = 745BE24FE1467192   390223 B
scratch          = 2D61519C6544E3C1   349759 B
grade_half       = 平场：min = max = 188（1 种颜色，614400/614400）  ← strength = 0.5 在纯反相上数学恒为 0.5

## 五、已丢失、且**找不回来**的

anchor exe             = D7ED54FDB8323EDD…  (被钉住的那一支；冻结的构建产物，重建不是同一个字节序列)
# 它的读数都在上面；但"向 oracle 提问"那个能力随之消失（§143 靠它测出透明相位的次序规则）。
# 要用就得重建一支，而重建的那支**必须先把这六张判据图重出一遍、逐字节对上**，才够格当 oracle。

# ⚠ §157 就地更正（2026-09-19，三条并存的事实；`README.md` 有全文）：
#   ① 上面那一格**仍然成立**：被钉住的那支 `D7ED54FDB8323EDD…` 已丢失且不可重建。
#   ② ⚠ 但"能力随之消失"这句**在写下时不成立**：2026-09-19 实测 `target/debug/px_render.exe`
#      （`EB8F85452C8B2E291B6714BA04325AF784565F7F335E4BC3265E472F68CE38C7`，164,638,720 B）
#      是一支**还能跑的 Bevy 宿主**，六份冻产物出图与 §一 那六张**逐字节全中**（0/6 失败；
#      仪器 `target/pre-rename/bevy-six.ps1`）⇒ 按上面那句自己立的门槛，**它够格**。
#   ③ 而**裁决是不留**（用户：「该做的功能都做完了，留着 oracle 干嘛」）—— 靶子在 git、
#      源码在 `f121ee3^` ⇒ 留 164 MB 是给一个不存在的消费者留。改名 `px_render_wgpu → px_render`
#      覆盖了那个路径（覆盖是对的：`harness.ps1` 的 `$HarnessExe` 就指它）⇒ **§157 起 ② 那句成立**。
#      ⚠ 那份副本 `target/pre-rename/px_render-bevy-EB8F8545.exe`（sha256 与原件逐字节同）
#      **是未登记的临时副本，不是资产** —— 别依赖它。
#   拿回来的路：源码完整在 `f121ee3^`（`px_render/` 21 个文件，含 `Cargo.toml` 与 `tests/`），
#   bevy 一族的锁在那一笔的 `Cargo.lock` 里 ⇒ 命令与注意事项见 `15-render-wgpu.md` §157.5。

## 六、⚠ 盘上字节的**行尾约定**：这一格能不动一个像素而换掉所有键（S8-c 立，见 §156）

# 为什么它属于这份表：闭包指纹哈希的是**模块源码字符串**（`px_shader/src/lib.rs` 的
# `Closure::fingerprint`），而源码是**从盘上读的** ⇒ 盘上那批字节是**每一个产物键的输入**，
# 也就是 §三 那六份冻产物"文件字节"的上游。换一份 checkout 约定不同的副本，可能：
#   像素一个不动（§155.3 已证），而 §三 六格当场全红。

# 量法（三步，S8-c 2026-09-19 在本工作副本上实跑；三条都不需要动 art/ 下一个字节）：
#   ① 粗看：git ls-files --eol art/shaders art/frame
#        i/lf + w/crlf ⇒ index 里是 LF、盘上是 CRLF（两种 checkout 会给不同字节）
#        i/lf + w/lf   ⇒ 盘上是 LF（但**本机重新 checkout 可能给它 CRLF**，见 ③）
#   ② 核字节：git ls-files -s <文件> → blob sha；git cat-file -s <blob> → blob 字节数；
#        stat -c%s <文件> → 盘上字节数。两者之差恰等于行数 ⇒ 一边 CRLF 一边 LF。
#   ③ 决定性的那一步：git checkout-index --prefix=target/<某处>/ -- <文件>
#        ⇒ 写出"**本机重新 checkout 会给的那份字节**"，与盘上直接比。
#        加 `git -c core.autocrlf=false checkout-index …` ⇒ 写出"**无转换的机器会给的那份**"。

# ⚠ core.autocrlf=true（来源：file:C:/SoftWare/git/Git/etc/gitconfig，**机器级**不是仓库级）。

# 实测（本副本，2026-09-19）：差 0 = 盘与 index 是同一批字节
#   art/shaders/surface.wgsl      盘 9780 ／ index 9603  差 177 ⇒ **盘 CRLF**
#   art/shaders/clouds.wgsl       盘 35364 ／ index 34587 差 777 ⇒ 盘 CRLF
#   art/shaders/atmosphere.wgsl   盘 2175 ／ index 2122  差 53  ⇒ 盘 CRLF
#   art/shaders/ring.wgsl         盘 1035 ／ index 1014  差 21  ⇒ 盘 CRLF
#   art/shaders/lib/{common,light,noise}.wgsl  差 59 ／ 85 ／ 164 ⇒ 盘 CRLF
#   art/shaders/{blit,px_grade,px_vignette}.wgsl 差 0 ⇒ 盘 **LF**
#   art/frame/{skybox,vertex_mesh,vertex_sky}.wgsl 差 0 ⇒ 盘 **LF**
#   art/frame/default.toml 差 194、art/scene/orbit-bare.toml 差 26 ⇒ 盘 CRLF
#   art/scene/orbit-bare-nolight.toml / orbit-bare-shadow.toml 差 0 ⇒ 盘 LF
# ⇒ **不是"这份副本全是 CRLF"**。同一棵树下两种行尾并存 ⇒ 只能一份一份量。

# ③ 的读数（本机 checkout ／ 无转换 checkout ／ 现在盘上）：
#   art/shaders/surface.wgsl    9780 ／ 9603 ／ 9780     本机=盘（吻合）
#   art/shaders/blit.wgsl       1581 ／ 1554 ／ 1554     ⚠ **本机重 checkout 会把它变成 CRLF**（1554 → 1581）
#   art/frame/skybox.wgsl       7136 ／ 7030 ／ 7030     ⚠ 同上（7030 → 7136）
#   art/frame/default.toml     10583 ／ 10389 ／ 10583   本机=盘
#   art/scene/orbit-bare.toml    867 ／ 841 ／ 867       本机=盘
# ⇒ **两个方向都会咬人**：
#   换到无转换的机器（Linux / CI）：内容入口与库（surface/clouds/atmosphere/ring/lib/*）
#     从 CRLF 变 LF ⇒ 闭包指纹变 ⇒ 产物键变 ⇒ §三 那六格的文件字节跟着变（**破的是逃生门**）。
#   在本机重 checkout：今天在盘上是 LF 的那几份**都会变大**（逐份实测，见 ③ 与下面这行）——
#     blit 1554→1581、px_grade 1002→1023、px_vignette 1095→1117、
#     frame/{skybox 7030→7136、vertex_mesh 6769→6883、vertex_sky 2006→2043}
#     ⇒ 帧材质的内容键变 ⇒ 带帧图的 pass 文档字节变（**J3 的图不变、文档字节变**）。
# ⚠ 后一条 `git status` **看不见**：CRLF 与 LF 两份 clean 之后是同一个 blob
#   ⇒ "git 说我干净"与"字节没变"是两件事（§122 那条"验的是哪个产物"的同一族）。

# ⚠ 这一格**只量了字节数**。"键会变"是 §155.1 机制的直接推论（指纹哈希的是源码字符串，
#   输入不同 ⇒ 键重算），本单元**没有**在另一份约定的副本上重烘一遍去实测它 ——
#   那要动 art/ 下的字节，是明令禁止的（§155.4）。所以这里是**已量的前提 + 标明来源的推论**。

# ⚠ 报警器：`px_shader` 那条判据测试（四份入口的**组装字节数 + FNV + 闭包指纹**，
#   `the_four_entry_shaders_assemble_to_these_bytes_under_the_wgpu_host_table`）读的就是盘上这份文本
#   ⇒ 换约定之后它会**先红**，而且说的是"字节数变了"。别把它当噪声改掉。

# 今天"全绿"的前提，逐条说清（两条都是量出来的，不是假设）：
#   1) **六份冻产物**：本单元在本副本上重烘，文件字节与 §三 逐字节相同
#      ⇒ 内容入口的字节约定**自冻结以来没有漂过**（键相同才会字节相同）。
#   2) **六张判据图**：图不比文本字节 —— 行尾不改像素（§155.3 把"像素与文本无关"量过一遍）。
#   ⇒ 前提是"**产物那一半**可证同约定"；**不是**"锚和我在同一种约定下量的"这种无法检验的话。
#   ⚠ 锚 exe 已丢失（§五），"向 oracle 提问"的能力不可恢复 —— 所以这条前提只能这样立。
